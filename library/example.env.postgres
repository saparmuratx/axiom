POSTGRES_USER=your_user
POSTGRES_PASSWORD=your_password
POSTGRES_DB=your_db_name
POSTGRES_INITDB_ARGS=--encoding=UTF-8

POSTGRES_PORT=your_port

# Application DATABASE_URL — adjust driver if using async (asyncpg) or sync drivers
# For synchronous SQLAlchemy: postgresql://user:password@host:port/dbname
# For async SQLAlchemy with asyncpg: postgresql+asyncpg://user:password@host:port/dbname

DATABASE_URL=postgresql://your_user:your_password@localhost:your_port/your_db_name
